# CLAUDE.md — Mission Control Dashboard

## Project
React/Vite dashboard deployed on Vercel.
URL: https://mission-hub-iota.vercel.app
GitHub: github.com/crypto86m/mission-hub (PUBLIC repo)

## Tech Stack
- React 18 + Vite
- Tailwind CSS
- Supabase (Pro tier) for real-time data
- Vercel for hosting (auto-deploy on push to main)

## Supabase
- URL: https://uunqqpqyehxjodvozlgu.supabase.co
- Anon key: In .env.local
- Tables: agent_status, approvals, activity_feed, cost_tracking, trading_pnl

## Screens (16 total)
Dashboard, Tasks, Calendar, Agents, Workflows, Settings, Integrations,
Intelligence, Scorecard, Content, Memory, Instagram, Social, Editing,
Reels, Approvals

## Data Sources
- /public/api/status.json — Main data feed (generated by scripts)
- Supabase tables — Real-time via subscriptions
- Local APIs: Gateway restart (port 4345), Booking intake (port 4344)

## Key Components
- GatewayControl.jsx — Restart button + status (port 4345 API)
- MasterConnect.jsx — Connection diagnostics
- SystemHealthWidget.jsx — System health overview
- NotificationSystem.jsx — Alert delivery

## Deploy
```bash
git add -A && git commit -m "description" && git push origin main
```
Vercel auto-deploys from main branch.

## Rules
- No fake/hardcoded data — everything from real sources
- If data unavailable, show "Awaiting data" not a fake number
- Test locally with `npm run dev` before pushing
- Keep components under 300 lines
